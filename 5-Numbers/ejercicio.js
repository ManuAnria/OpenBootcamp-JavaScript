let altura_cm = 186;
let altura_mt = 1.86;
let peso = 80.4;

let alt_round = Math.ceil(altura_mt);
let peso_round = Math.floor(peso);

let comp = (Number.MAX_VALUE + 1) === Number.MAX_VALUE;
console.log(comp);

//Notacion
// ; -> separador de sentencias, para separar una linea de codigo de otra. NO ES NECESARIO PONERLO

const b = 2;

// . -> para acceder a las propiedades(ATRIBUTOS) de un objeto

// [] -> para crear o acceder a los elementos de un array(listas, arreglos)
const ar = [1, 2, 3, 4, 5];
console.log(ar[0]); 

// () -> para funciones
function suma(a, b) {
    return a + b;
}

// {} -> para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10,
}

if (true) {
    console.log("Hola");
}
